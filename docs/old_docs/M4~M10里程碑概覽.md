## 三、開發里程碑（M4-M10）

每個 milestone 含：目標、關鍵任務、新增/修改檔案、驗收檢查、估時、依賴。

---

### M4 — 錄製儀式骨架

| 項目 | 內容 |
|---|---|
| 涵蓋系統 | S1, S2, S3, S4（最低版） |
| 估時 | **1-2 週** |
| 前置依賴 | M3 完成 |
| 後續依賴 | M5 需要本 milestone 的 PlayerController |

#### 目標
把現有「玩家邊走邊錄」改成「按 R → 本體留原地、新分身出現、玩家操控分身」。
最低版視覺儀式可運作。

#### 關鍵任務
1. 創建 PlayerController autoload
2. 修改 Player.gd：加 is_frozen flag、_physics_process 早退邏輯
3. 創建 RecordingProxy.gd + RecordingProxy.tscn
4. 連結 PlayerController 與 RecordingManager
5. M4 最低版 RitualPlayer：暫停 0.2s、淡入淡出 0.3s、紅框
6. 本體凍結時 collision/物理處理

#### 新增檔案
- `godot/scripts/PlayerController.gd`
- `godot/scripts/RecordingProxy.gd`
- `godot/scenes/RecordingProxy.tscn`
- `godot/scripts/RitualPlayer.gd`

#### 修改檔案
- `godot/scripts/Player.gd` — 加 freeze/unfreeze、凍結時邏輯
- `godot/project.godot` — 加 PlayerController autoload

#### 驗收檢查
- [ ] 按 R 後本體靜止半透明、RecordingProxy 出現可控
- [ ] 玩家走 RecordingProxy 期間 RecordingManager 正常記錄
- [ ] 按結束鍵後 RecordingProxy 消失、本體恢復
- [ ] 全程不崩潰、輸入正確路由
- [ ] 自評：「我感受到本體與分身的分離了嗎？」≥ 6/10（最低版可接受）

---

### M5 — 光點系統 + 5 槽位

| 項目 | 內容 |
|---|---|
| 涵蓋系統 | S5, S6 |
| 估時 | **1 週** |
| 前置依賴 | M4 |
| 後續依賴 | M6 需要 LightPoint |

#### 目標
錄製結束時生成光點。RecordingManager 改為 5 槽位 + FIFO。

#### 關鍵任務
1. 創建 LightPoint 場景與類別
2. 改寫 RecordingManager 為 5 槽位
3. FIFO 邏輯（含舊光點轉痕跡）
4. PlayerController 在 end_recording 時呼叫光點生成
5. 通過 signal 通知 HUD 槽位變化

#### 新增檔案
- `godot/scripts/LightPoint.gd`
- `godot/scenes/LightPoint.tscn`

#### 修改檔案
- `godot/scripts/RecordingManager.gd` — 5 槽位 + FIFO
- `godot/scripts/PlayerController.gd` — 加光點生成邏輯

#### 驗收檢查
- [ ] 錄製結束後光點出現在錄製起點
- [ ] 5 個錄製後第 6 個觸發 FIFO，槽位 0 光點轉痕跡
- [ ] 痕跡光點不脈動、亮度 25%
- [ ] HUD（既有版）顯示槽位 1-5 標籤

---

### M6 — 光點互動 + HUD 重寫

| 項目 | 內容 |
|---|---|
| 涵蓋系統 | S7, S14 |
| 估時 | **1 週** |
| 前置依賴 | M5 |
| 後續依賴 | M10 視覺打磨會再動 HUD |

#### 目標
站光點上的 R 重錄、Q 刪除互動。HUD 重寫為 5 槽位顯示 + 上下文提示。

#### 關鍵任務
1. PlayerController 偵測站光點事件
2. R 重錄邏輯：snap 到光點中心、覆蓋對應槽位
3. Q 刪除邏輯
4. HUD 重寫：5 槽位列表 + 上下文提示
5. 改寫 [`CloneSpawner.gd`](godot/scripts/CloneSpawner.gd) 改用槽位索引召喚

#### 修改檔案
- `godot/scripts/CloneSpawner.gd` — 改用槽位索引
- `godot/scripts/HUD.gd` + `godot/scenes/HUD.tscn` — 重寫
- `godot/scripts/PlayerController.gd` — 加光點互動處理
- `godot/project.godot` — 加 delete_slot 輸入動作

#### 驗收檢查
- [ ] 站亮光點上按 R 進入重錄、覆蓋該槽位
- [ ] 站亮光點上按 Q 刪除、光點消失
- [ ] 站痕跡光點上無互動
- [ ] HUD 5 槽位顯示正確、上下文提示淡入淡出

---

### M7 — Frozen Clone

| 項目 | 內容 |
|---|---|
| 涵蓋系統 | S8 |
| 估時 | **0.5-1 週** |
| 前置依賴 | M4, M6 |

#### 目標
錄製期間既有分身凍結成模糊剪影、保留世界影響。

#### 關鍵任務
1. Clone.gd 加 freeze/unfreeze
2. PlayerController 在 start/end_recording 呼叫所有 active Clone
3. RecordingProxy 的 collision_mask 排除 layer 2

#### 修改檔案
- `godot/scripts/Clone.gd`
- `godot/scripts/RecordingProxy.gd`
- `godot/scripts/PlayerController.gd`

#### 驗收檢查
- [ ] 錄製開始所有 active Clone 0.3 秒淡化為灰色剪影
- [ ] 凍結中 Clone 仍壓按鈕（門保持開啟）
- [ ] RecordingProxy 對凍結 Clone 穿透
- [ ] 結束錄製 Clone 解凍恢復播放

---

### M8 — 殘骸系統

| 項目 | 內容 |
|---|---|
| 涵蓋系統 | S9, S10, S11 |
| 估時 | **1.5-2 週** |
| 前置依賴 | M7 |

#### 目標
分身被機關殺死形成殘骸；CarcassManager 槽位制；新/舊痕跡兩種樣式。

#### 關鍵任務
1. PhysicalCarcass + CarcassTrace 場景與類別
2. CarcassManager autoload
3. Hazard 類別 + 場景（先做尖刺）
4. Clone.gd 加 die() 方法、Hazard 偵測 Clone 觸發
5. CarcassTrace 的新/舊兩階段視覺
6. PressButton 偵測 PhysicalCarcass

#### 新增檔案
- `godot/scripts/PhysicalCarcass.gd` + `godot/scenes/PhysicalCarcass.tscn`
- `godot/scripts/CarcassTrace.gd` + `godot/scenes/CarcassTrace.tscn`
- `godot/scripts/CarcassManager.gd`
- `godot/scripts/Hazard.gd` + `godot/scenes/HazardSpike.tscn`

#### 修改檔案
- `godot/scripts/Clone.gd` — 加 die()
- `godot/scripts/PressButton.gd` — filter 加上 StaticBody2D（殘骸可壓）
- `godot/project.godot` — 加 CarcassManager autoload

#### 驗收檢查
- [ ] Clone 撞尖刺死，原位置出現物理殘骸
- [ ] 殘骸可踩、可壓按鈕
- [ ] 同槽位 Clone 連續死，地圖只看到 1 個物理殘骸
- [ ] 60 秒後痕跡從新（50%）變舊（20%）
- [ ] 主動犧牲玩法可運作

---

### M9 — TriggerZone + 重置 + A3

| 項目 | 內容 |
|---|---|
| 涵蓋系統 | S12, S13, S15 |
| 估時 | **1 週** |
| 前置依賴 | M8 |

#### 目標
TriggerZone 系統。本體死亡觸發 zone 範圍重置。RecordingProxy 死亡（A3）整合。

#### 關鍵任務
1. TriggerZone 類別與場景
2. PlayerController 訂閱 Player.died signal
3. CarcassManager.reset_zone 邏輯
4. A3：Hazard 偵測 RecordingProxy → 結束錄製儀式
5. 最短錄製時長 0.5 秒判斷

#### 新增檔案
- `godot/scripts/TriggerZone.gd` + `godot/scenes/TriggerZone.tscn`

#### 修改檔案
- `godot/scripts/Player.gd` — 加 died signal
- `godot/scripts/PlayerController.gd` — 加重置事件處理 + A3
- `godot/scripts/Hazard.gd` — 偵測 Player / RecordingProxy 分流

#### 驗收檢查
- [ ] Player 死亡觸發黑屏 + 重生
- [ ] 重生時當前 zone 內殘骸轉痕跡，跨 zone 不變
- [ ] active Clone 全部消失
- [ ] RecordingProxy 撞刺，錄製存槽位 + 殘骸生成
- [ ] 0.5 秒以內的錄製不存

---

### M10 — 視覺儀式打磨 + 第一謎題重做

| 項目 | 內容 |
|---|---|
| 涵蓋系統 | S4 提升 + 跨系統整合 |
| 估時 | **1.5-2 週** |
| 前置依賴 | M9 |

#### 目標
視覺儀式從 M4 最低版升級到完整版。第一謎題重做以對應新機制。

#### 關鍵任務
1. RitualPlayer 升級：完整 0.9/1.0 秒儀式
2. 鏡頭平滑過渡（Camera2D position_smoothing）
3. 紅框升級為膠卷邊框風格
4. （可選）模糊 shader for Frozen Clone
5. FirstPuzzle.tscn + .gd 重做：適應新機制（本體留原地、5 槽位等）
6. 移除 HintManager（被新 HUD 上下文提示取代）

#### 修改檔案
- `godot/scripts/RitualPlayer.gd` — 升級儀式
- `godot/scenes/FirstPuzzle.tscn` — 重做
- `godot/scripts/FirstPuzzle.gd` — 重做
- 移除：`godot/scripts/HintManager.gd`

#### 驗收檢查
- [ ] 完整儀式時長感受對（自評 ≥ 7/10）
- [ ] 第一謎題用新機制可解
- [ ] 找朋友試玩，30 分內理解核心機制
- [ ] **主題**: 「我感受到時間分裂了」自評 ≥ 7/10
