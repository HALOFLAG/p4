# 音效系統規劃（尚未實作）

> 本文件規劃音效系統的整體架構與實作步驟，**本回合不寫程式**。
> 之後決定要做時、照本文順序進行。

---

## 一、為什麼要音效

目前遊戲零音效。試玩反饋幾個高機率出現的問題：
- 「按 R 沒感覺、按了不知道有沒有觸發」 → 缺少**啟動音**
- 「分身在動但我沒注意到」 → 缺少**召喚音**
- 「分身死了我才發現」 → 缺少**死亡音**
- 「房間切換沒有過渡感」 → 缺少**環境氛圍**

音效是**主題傳達的低成本放大器**：cinematic 儀式感最後 30% 靠聲音。

---

## 二、Godot 4 音效系統基礎

### 三個核心節點

| 節點 | 用途 |
|---|---|
| `AudioStreamPlayer` | 純 2D UI 音效、無位置、整體播放（適合按鈕、UI、BGM）|
| `AudioStreamPlayer2D` | 2D 世界音效、有位置、依距離衰減（適合環境音、機關聲）|
| `AudioStreamPlayer3D` | 3D 音效（本專案是 2D 不需要）|

### 音訊資源
- `.wav`：未壓縮、體積大、低延遲、適合短 SFX
- `.ogg`：壓縮、體積小、適合 BGM 與長音效
- 匯入 Godot 後變成 AudioStream resource、可在 inspector 設 loop / volume / 標籤

### Bus 系統
Godot 有「audio bus 路由」可獨立調音量、加效果（reverb, EQ）。本專案推薦：
- **Master**（總音量）
- **Music**（BGM）
- **SFX**（音效）
- 玩家設定畫面可獨立調 Music / SFX 音量、不影響彼此

設定位置：Project → Audio → Audio Buses Layout（編輯器底部 Audio panel）

---

## 三、本專案推薦架構

### 方案：AudioManager autoload + 命名查表

```gdscript
# AudioManager.gd（新 autoload）
extends Node

# SFX 資源預先 preload，呼叫時 .play() 即可
const SFX := {
    "button_press": preload("res://audio/sfx/button_press.wav"),
    "jump":         preload("res://audio/sfx/jump.wav"),
    "land":         preload("res://audio/sfx/land.wav"),
    "record_start": preload("res://audio/sfx/record_start.wav"),
    "record_end":   preload("res://audio/sfx/record_end.wav"),
    "summon":       preload("res://audio/sfx/summon.wav"),
    "die":          preload("res://audio/sfx/die.wav"),
    "carcass":      preload("res://audio/sfx/carcass.wav"),
    "door_open":    preload("res://audio/sfx/door_open.wav"),
    "door_close":   preload("res://audio/sfx/door_close.wav"),
    "checkpoint":   preload("res://audio/sfx/checkpoint.wav"),
    "exit":         preload("res://audio/sfx/exit.wav"),
}

# Pool 設計：固定 N 個 AudioStreamPlayer，輪流使用避免重疊截斷
const POOL_SIZE := 8
var _pool: Array[AudioStreamPlayer] = []
var _next_idx := 0

func _ready():
    for i in POOL_SIZE:
        var p := AudioStreamPlayer.new()
        p.bus = "SFX"
        add_child(p)
        _pool.append(p)

func play_sfx(name: String, volume_db: float = 0.0) -> void:
    if not SFX.has(name):
        push_warning("[Audio] 找不到 SFX: %s" % name)
        return
    var p := _pool[_next_idx]
    _next_idx = (_next_idx + 1) % POOL_SIZE
    p.stream = SFX[name]
    p.volume_db = volume_db
    p.play()
```

呼叫超簡單：
```gdscript
AudioManager.play_sfx("button_press")
AudioManager.play_sfx("jump", -3.0)  # 小聲一點
```

### BGM
單獨 `AudioStreamPlayer` 在 `AudioManager`、設 `bus="Music"` 與 `loop=true`：
```gdscript
@onready var bgm: AudioStreamPlayer = $BGM

func play_bgm(name: String):
    bgm.stream = BGM_LIBRARY[name]
    bgm.play()
```

---

## 四、音效清單（最小可用組）

| ID | 場景 | 風格建議 | 優先 |
|---|---|---|---|
| `button_press` | 按鈕被踩下 | 短促 click（金屬扣 / 木頭按下感）| ⭐⭐⭐ |
| `jump` | 玩家跳躍 | 輕快的「啵」 | ⭐⭐⭐ |
| `land` | 落地 | 悶悶的「噹」 | ⭐⭐ |
| `record_start` | R 開始錄製 | 神秘的「嗡」、低頻啟動 | ⭐⭐⭐ |
| `record_end` | R 結束錄製 | 收束感、輕柔的「叮」 | ⭐⭐⭐ |
| `summon` | 召喚分身 | 幽魂出現感、dreamy bell | ⭐⭐⭐ |
| `die` | 本體 / Clone 死亡 | 短促失敗音（不要太刺耳）| ⭐⭐ |
| `carcass` | 殘骸生成 | 沉悶「咚」、有重量感 | ⭐⭐ |
| `door_open` | 門開始開啟 | 機械齒輪 / 沉重金屬 | ⭐⭐ |
| `door_close` | 門關閉 | 同上但下行 | ⭐ |
| `checkpoint` | 旗幟啟用 | 溫暖鈴鐺、安全感 | ⭐⭐ |
| `exit` | 通關到達 Exit | 上揚 fanfare 短旋律 | ⭐⭐⭐ |
| `bgm_main` | 全程環境 BGM | 安靜緩慢的 ambient pad、低 BPM | ⭐⭐ |

13 個 SFX + 1 BGM = MVP。可以之後加房間特定 BGM、儀式專用音層。

---

## 五、資源來源（免費）

優先免費庫、後期需要客製再外發：

| 來源 | 內容 | 授權 |
|---|---|---|
| [Freesound.org](https://freesound.org) | 海量使用者上傳 SFX | CC0 / CC-BY，要看單檔 |
| [Kenney Audio Packs](https://kenney.nl/assets?q=audio) | 整套 SFX 包，遊戲導向、品質穩定 | CC0 |
| [Sonniss GDC Bundle](https://sonniss.com/gameaudiogdc) | 每年 GDC 釋出的 30GB+ 大包、商用免費 | 自由商用 |
| [OpenGameArt.org](https://opengameart.org/art-search?keys=&field_art_type_tid%5B%5D=13) | Audio 分類 | 多種授權 |
| [Pixabay Music](https://pixabay.com/music) | BGM、ambient | CC0 |

**搜尋技巧**：「ui click」「magical chime」「sci-fi door」「ambient pad loop」等英文關鍵字命中率高。

---

## 六、實作順序建議

### Phase A — 基礎建置（半天）
1. Audio bus 設定（Master / Music / SFX）
2. 建 `audio/sfx/` 與 `audio/bgm/` 資料夾
3. AudioManager.gd autoload + 8 顆 SFX player pool
4. 註冊到 project.godot

### Phase B — 核心 SFX（1 天）
1. 找 / 製作 13 個 SFX 檔案
2. 各場景 hook：
   - PressButton._on_body_entered → play_sfx("button_press")
   - Player._apply_movement 跳躍時 → "jump"; is_on_floor 從 false→true → "land"
   - PlayerController.start_recording → "record_start"
   - PlayerController.end_recording → "record_end"
   - CloneSpawner.spawn_clone → "summon"
   - Player.die / Clone.die → "die"
   - CarcassManager.register_death → "carcass"
   - Door.set_target(true / false) → "door_open" / "door_close"
   - Checkpoint._activate → "checkpoint"
   - Exit reached → "exit"

### Phase C — BGM（半天）
1. 找一首 ambient pad（loop 60s+）
2. AudioManager.play_bgm("main") 在 World._ready 啟動
3. 之後可加：錄製期間 BGM 切換到「reduced layer」（用 AudioServer.set_bus_volume_db 動態降）

### Phase D — 設定畫面（半天，等標題畫面做）
1. 主選單加 Music/SFX volume sliders
2. 連到 AudioServer.set_bus_volume_db("Music"/"SFX", linear_to_db(slider.value))
3. 存到 user://audio_settings.cfg

### Phase E — 細節調音（持續迭代）
1. 設 SFX 隨機音調（pitch_scale 0.95-1.05）避免重複死板
2. 加 reverb bus 給某些音層（房間切換時）
3. 儀式期間 lowpass filter 整體音場（突顯「進入時間裂縫」感）

---

## 七、估時與風險

### 估時
- Phase A：4-6 小時
- Phase B：1-2 天（找 SFX 是大頭）
- Phase C：2-4 小時
- Phase D：4-6 小時（含 UI）
- Phase E：持續迭代、依需要
- **MVP 整套（A+B+C）**：2-3 天

### 風險
- ⚠️ **音效搜尋耗時**：找一個合適 SFX 可能花 30 分鐘+。建議一次集中找完、不要做一個找一個
- ⚠️ **過多音效干擾**：每個動作都有音效會吵；只給「玩家需要回饋的事件」加音
- ⚠️ **GL Compatibility 平台音效相容**：基本上沒問題、Godot audio 是引擎層、不依賴 renderer
- ⚠️ **Pool size 太小**：高頻事件（同時多個按鈕被按）會截斷舊音；POOL_SIZE 8 應夠

### 不要踩的雷
- ❌ 用 streaming 音檔做 SFX（loading 慢、有延遲）→ 用 .wav
- ❌ 在每個節點各自建 AudioStreamPlayer → 用 AudioManager 集中
- ❌ BGM 不開 loop → 播完會靜音
- ❌ 直接 set volume = 0 來「靜音」→ 用 bus volume 控制、保留可恢復

---

## 八、之後新增此文件變更時、記在這

| 日期 | 變更 |
|---|---|
| 2026-05-02 | 文件創建。包含架構、清單、來源、6 階段實作順序 |
