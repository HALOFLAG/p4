# 謎題提案 — 設計工作簿

> 收集後續房間（Room 5+）的謎題設計、機制變化、難度評估。
> Room 1-4 教學內容見 [`godot/scenes/World.tscn`](godot/scenes/World.tscn) 與 [`設計構想.md`](設計構想.md)。

## 一、現有機制工具箱

設計新謎題前，先盤點 M4-M10 已實作的零件：

### 角色與分身
| 元素 | 行為 | 設計用途 |
|---|---|---|
| **本體 Player** | 玩家直接操控；錄製中凍結成半透明剪影 | 「當下的我」 |
| **RecordingProxy** | 錄製期間玩家控制的「鏡頭分身」 | 製造錄製內容 |
| **Clone** | 召喚 1-5 重播錄製；凍結期間仍會壓按鈕 | 過去的自己合作 |
| **PhysicalCarcass** | Clone 撞 hazard 留下的物理屍體；可踩、可壓按鈕 | 主動犧牲、平台、按鈕鎮重 |
| **CarcassTrace** | 殘骸轉化的視覺痕跡；無 collision、永久存在；60s 後變舊 | 記憶、敘事 |

### 互動物件
| 元素 | 行為 |
|---|---|
| **PressButton** | Area2D；Player/Clone/Proxy/Carcass 站上即觸發；鬆開即關；視覺有壓下動畫 |
| **PressCounter** | Node；聚合 N 顆 PressButton 壓下次數、**≥ target_count**（不需精準）即 emit `target_reached`、可連 Door；達標 latch | 計次型謎題（多次踩按鈕、多分身重複按、跨房共用）|
| **Door** | StaticBody；`set_target(true/false)` 控制升降；視覺 top 沉入地面；`door_height` 可調 |
| **Hazard** | Area2D；Clone → die + 殘骸；Proxy → A3 結束錄製；Player → 黑屏重生 |
| **HazardSpike** | Hazard 的具體實現（紅三角尖刺）|
| **TriggerZone** | 範圍偵測；Player 死亡時該 zone 內殘骸轉痕跡 |
| **LightPoint** | 錄製結束生成；BRIGHT/TRACE 兩態；R 重錄、Q 刪除 |
| **Checkpoint**（旗幟） | Player 碰到 → 設成新重生點；金黃 = active、暗藍灰 = inactive |
| **Sign** | 接近淡入文字提示 |
| **Room / RoomManager** | 一房一鏡頭、平滑切換；Bounds 偵測 player |
| **DeathBand** | 全域 Hazard 帶、掉出畫面外 → die |

### 系統規則
- 5 槽位（時戳 FIFO；OLD/NEW 標籤）
- 錄製 0.5-45s 區間（短於丟、超於自動結束）
- 每槽殘骸獨立 collision layer（同 slot 後續分身穿過自己舊殘骸）
- 分身離開錄製起始房間 → 自動結束錄製
- 跨房槽位保留

---

## 二、已實作的教學房間（Room 1-4）

| Room | 主題 | 核心新機制 |
|---|---|---|
| 1 | 移動跳躍 | 純操作熱身、S 形跳台 |
| 2 | 按鈕跳門 | PressButton + Door + 時機 |
| 3 | 第一次錄製 | R 錄 + 1 召喚 + 分身合作 |
| 4 | 雙按鈕雙分身 | 多錄製 + 同時召喚 + AND-gate |

---

## 三、新機制：拉桿（Lever）

> 設計目的：引入「**可被多次互動的開關**」，與按鈕（站上才有效）形成對比。

### 規格草案
- **節點**：Area2D + 視覺（拉桿圖示）
- **互動**：玩家或分身碰到時自動切換狀態（toggle），或按鍵互動
- **狀態**：兩態 toggle（A/B）；切換瞬間發 signal
- **冷卻**（可選）：拉一次後鎖死 N 秒避免抖動

### 與按鈕的差異
| | PressButton | Lever |
|---|---|---|
| 觸發條件 | 持續站著 | 接觸瞬間 toggle |
| 鬆開後 | 自動關 | 保持狀態 |
| 適合 | 持續性條件（門開到走過為止）| 永久性切換（地形 / 區段） |
| 分身用途 | 站著按住 | 路過拉一下就行 |

### 衍生變體
- **Hold-Lever**：要被持續推著（同按鈕的拉桿視覺版）
- **Cooldown-Lever**：拉一次冷卻 N 秒
- **重物-Lever**：要被殘骸或角色壓住才生效（殘骸新用途）
- **節奏-Lever**：可被連續多次拉動、產生 A/B 交替（高難度）

---

## 四、Room 5+ 提案

### Room 5 — 拉桿入門（推薦）

**主題**：toggle 拉桿 + 兩個拉桿（一個本體可達、一個分身可達）

```
┌──────────────────────────────────────────┐
│                                          │
│  拉桿 2 (本體可達)─┐         Exit       │
│                   │                      │
│  ─────A 段平台──── ─────B 段平台─────  ──│
│                                          │
│  ▲CP                                     │
│  本體出生              拉桿 1 (本體不可達)│
└──────────────────────────────────────────┘
```

**解法**：
1. 玩家錄製：走到拉桿 1（高處 / 跨溝）、拉動 → 結束錄製
2. 召喚分身 → 分身去拉拉桿 1 → A 段平台出現
3. 本體走過 A 平台到中段
4. 本體拉拉桿 2 → 切到 B 段平台
5. 走過 B 段到 Exit

**教學內容**：拉桿是 toggle、拉桿可以由「分身拉一次就走」（不像按鈕需要持續站著）

**難度**：⭐⭐ 低
**操作**：低；**設計**：低

---

### Room 6 — 計次門（已實作 ✅）

**實際實作**（2026-05-02）：
- 目標 50 次、Room 6 內 2 個按鈕、`track_all_buttons=true` 吃所有房間按鈕
- `reset_on_checkpoint=true` 碰任何旗幟（含 respawn）會歸零、Door 關回
- CounterLabel 在房間頂顯示「X / 50」
- Exit 移到 Room 6（取代原本 Room 4 的 Exit、Room 4 RightWall 移除讓玩家可進 Room 6）

**設計意圖**：強迫玩家錄循環按按鈕、累計次數；高目標（50）+ 重置機制（碰旗幟）= 高挑戰

**注意點**：
- 玩家從 Room 4 走進 Room 6 會立刻碰到 Room 6 旗幟、計數歸零
- 如果死亡 respawn 回旗幟、計數又歸零
- 因此玩家要小心避開旗幟、或刻意先觸發旗幟設好 spawn 再開始累計

---

### Room 6A — 計次門（原始提案、已實作）

**主題**：用 PressCounter 製作「按 N 次才開」的門

```
┌──────────────────────────────────────────┐
│                                          │
│  Counter: 3 / 5                          │
│  [按鈕1]    [按鈕2]    [按鈕3]    Door──►│
│  ─────────────────────────────────────── │
│  ▲CP                                Exit │
└──────────────────────────────────────────┘
```

**解法 A**（單分身循環）：
- 玩家錄製：「走到按鈕 1 → 踩 → 走開 → 走回 → 踩 → 走開 → 踩按鈕 2 → ...」
- 召喚分身 → 分身重複踩 → 計數累加直到 5 → 門開

**解法 B**（多分身分散）：
- 錄三段、各到一顆按鈕踩一下
- 召喚 1 + 2 + 3 → 各自踩自己的按鈕、計數 +3
- 玩家自己手動踩兩次補滿 → 門開

**教學內容**：「分身可以做重複動作」、計數器 UI 的進度感

**難度**：⭐⭐⭐ 中
**操作**：中（解法 A 需錄循環）；**設計**：低

⚠️ **注意**：每次「壓下」算一次（鬆開不算），Player 站著不動不會多計。要再 +1 必須離開後重踩。

---

### Room 6B — 雙分身分階段切換

**主題**：單拉桿 + 兩個分身輪流拉

```
┌──────────────────────────────────────────┐
│                                          │
│  分身 1 路徑    │ 拉桿 │   分身 2 路徑   │
│  ───┐          │      │           ┌───   │
│     │      ───┘      └───              │ │
│                                          │
│  本體出生   ─A─────B─────C─    Exit     │
│  (要交替踩 A/B/C 段，A=拉桿 OFF, B=ON)   │
└──────────────────────────────────────────┘
```

**解法**：
1. 錄分身 1：走到拉桿、拉一下、待著
2. 錄分身 2：走到拉桿（從另一邊）、拉一下、待著
3. 本體召喚分身 1 → 拉桿 ON → 走 A 段
4. 走到中段、召喚分身 2 → 拉桿 OFF → 走 B 段
5. 重複至 Exit

**教學內容**：分身可以「分批使用」、同一機關可以被輪番控制

**難度**：⭐⭐⭐ 中
**操作**：中；**設計**：中

---

### 跨房共用機制延伸方向（PressCounter / 跨房狀態的設計池）

> Room 6 用 PressCounter + `track_all_buttons` + `reset_on_checkpoint` 開啟「跨房共用狀態」這個設計維度。設計上的目的：
> 1. **重用過去**：玩家錄過的分身在舊房間踩按鈕仍能幫助當前房間
> 2. **節省設計**：未來房間不必每個都放按鈕，借用既有房間的按鈕
> 3. **「世界是連通的」感受**：強化主題（過去自己持續影響當下）
>
> 重要設計約定：**計數判定一律「≥ 閾值」**（超過即達標，不需要精確匹配），避免變成密碼鎖式 puzzle。

#### A1. 多階段 milestone 開鎖（推薦 ⭐）
單一 counter 控制多個門：
- 30 → 中間捷徑門開
- 60 → 主門開
- 100 → 隱藏房間入口開

實作：PressCounter 改成 emit `count_milestone(threshold)` signals 而不是單一 `target_reached`。每個 Door 訂閱自己的 milestone。

#### A2. 跨房連動門（推薦 ⭐）
不用 counter、直接連：A 房的門訂閱 B 房按鈕的 `pressed_changed`。
- Room 7 進門卡住、玩家發現要跑回 Room 4 召喚分身按某按鈕、Room 7 門才開
- 強迫跨房思考、不只在當前房間找答案
- 實作零成本：World.gd 直接 `room_x_button.pressed_changed.connect(room_y_door.set_target)`

#### A3. 反向 counter（小心使用）
計數越高越糟：
- 「累積踩過 30 次」→ Hazard 開始出現
- 玩家學會「克制亂按」
- 違反直覺、需 puzzle 設計引導玩家發現此規則
- 實作：counter 達閾值 emit signal、訂閱者啟動 Hazard

#### B1. 主動 ResetButton（推薦 ⭐⭐）
新類別 `ResetButton`：玩家踩它就 `PressCounter.reset()`。
- 房間角落擺一個當「保險」、玩家策略失敗可主動歸零重來
- 與 checkpoint reset 結構相同、只差觸發點是按鈕
- 實作：類似 PressButton + 連到 PressCounter.reset()

#### B2. 殘骸計數器
新類別 `CarcassCounter`：累積場景中的物理殘骸數量、達 N 觸發機關。
- 鼓勵主動犧牲、設計「為大局而死」的 puzzle
- 殘骸轉痕跡時 -1（永久殘骸才算）
- 實作要 1 天：監聽 `CarcassManager.register_death` + 殘骸 queue_free 事件

#### B3. Cooldown 計數
按鈕加冷卻：`@export var press_cooldown: float = 3.0`、按一次後 N 秒不算。
- 強迫玩家**不能單一按鈕刷**、必須用不同按鈕或不同分身
- 自然產生「多按鈕、多分身」需求
- 實作：PressButton 加冷卻 timer

#### C1. 全域累積統計（推薦 ⭐⭐⭐ 主題核心）
不影響 puzzle、純記錄、結局可變：
- 累計按按鈕次數
- 累計死亡次數
- 累計召喚分身次數
- 累計錄製時長
- 通關後顯示「你死了 N 次、犧牲了 M 個分身」
- 結局對白依數字變化（「你慎重通關」vs「你犧牲了無數的自己」）

實作：autoload 一個 `GameStats` Node、計數 += 1 在各事件處呼叫。

#### C2. 「告解房間」（殘骸博物館）
累積死亡 X 次後解鎖一個彩蛋房間：
- 房間中是玩家所有殘骸的痕跡（CarcassTrace 集合）
- 純敘事、不解謎、像「你經歷的失敗」紀念碑
- 強化主題「過去的自己是有重量的」

#### C3. 房間進入次數計數
RoomManager.player_entered_room 累計、達 N 次改變狀態：
- 第一次進 Room 5 → 平台 X 在位置 A
- 第二次進 → 平台 X 在位置 B
- 設計「你必須回頭」的線性流程

#### C4. 全域召喚預算
不只 5 個槽位、加上「總召喚次數預算」：
- 每次召喚 -1、用完不能再召喚
- 玩家必須節省、思考「真的需要召喚才用」
- HUD 顯示「剩 N 次召喚」
- 通關需要的最低召喚數 = 這關的「optimal」分數
- ⚠️ 影響全 game balance、要等核心 puzzle 多再決定

---

### 不推薦的方向

- ❌ counter 跟敘事直接綁（「按到 N 次觸發對白」）：機制與敘事不該混
- ❌ counter 數字過大（>200）：會變成「按到睡著」、玩家挫折
- ❌ 看不見的 counter：玩家不知道進度 = 廢 puzzle
- ❌ 精準匹配（「剛好按 50 次、多一次就重來」）：密碼鎖式設計、極挫折

---

### Room 7 — 殘骸登場 + 主動犧牲

**主題**：第一個刻意死亡的關卡（教學殘骸機制）

```
┌──────────────────────────────────────────┐
│                                          │
│              坑           Exit           │
│  ────平台────  ↓ ↓ ↓ ↓ ↓ ──────────  ───│
│  ▲CP                ↓                    │
│                  尖刺地帶                │
│              （高頻尖刺、人不能跳過）   │
└──────────────────────────────────────────┘
```

**解法**：
1. 玩家錄製：走過坑、撞死在尖刺上（A3 觸發）
2. 該錄製存槽位、死亡點生**物理殘骸**
3. 召喚此槽位 → Clone 重播路徑 → 撞同一尖刺 → 又死、舊殘骸轉痕跡、新殘骸生在同位置
4. 多召幾次累積殘骸 → 殘骸把尖刺「填滿」可踩
5. 本體走殘骸過去到 Exit

**教學內容**：殘骸可被踩、主動犧牲填路、CarcassTrace 視覺意義

**難度**：⭐⭐⭐ 中
**操作**：低（重複召喚而已）；**設計**：高（殘骸填路是新概念）

---

### Room 8 — 拉桿節奏（你原本的版本）

**主題**：拉桿被分身週期拉動、A/B 平台交替、本體配合節奏

```
┌──────────────────────────────────────────┐
│                                          │
│         ────A───   ────A───              │
│              B          B                │
│              ────       ────             │
│  ▲CP    分身在拉桿區週期拉動    Exit    │
│         本體要踩 A/B 交替過去            │
└──────────────────────────────────────────┘
```

**解法**：
1. 錄製：走到拉桿、來回拉動 5-10 秒、結束
2. 召喚 → 分身重播拉桿節奏 → A/B 平台交替
3. 本體看分身節奏、跳上目前出現的平台、過去

**教學內容**：節奏配合、錄製是「行為腳本」可以包含週期動作

**難度**：⭐⭐⭐⭐ 高
**操作**：高（玩家自己錄節奏 + 配合跳）；**設計**：高

⚠️ **不適合早期房間**——玩家要熟悉拉桿（Room 5/6）+ 跳台（Room 1）後才能應付。

---

### Room 9+ — 跨機制組合方向

**9A. 殘骸壓拉桿**：
- 拉桿要被「重物壓住」才生效
- 玩家錄製 → 撞死 → 殘骸落在拉桿上 → 永久解鎖某機關
- 帶出殘骸的「永久效果」面向

**9B. 殘骸當壓門石**：
- 門需要被殘骸壓住才能保持開啟
- 玩家錄製、撞死壓在按鈕上 → 門永久開
- 主動犧牲的「為大局」感

**9C. 多分身合奏**：
- 3 個分身、3 個拉桿、要按特定順序拉
- 玩家分別錄三段、按 1/2/3 依序召喚

**9D. 跨房間殘骸繼承**：
- 在 Room A 死、殘骸留在 Room A
- 進 Room B 後再回 Room A、殘骸還在（檢查重置邏輯）
- 利用 Room A 殘骸開 Room B 才能用的東西（要設計傳送 / 共用機關）

---

## 五、難度曲線建議

```
Room 1: 跳躍                  ⭐
Room 2: 跳門                  ⭐⭐
Room 3: 第一錄製              ⭐⭐
Room 4: 雙分身                ⭐⭐⭐
─────── 已實作分隔線 ────────
Room 5: 拉桿 toggle 入門      ⭐⭐
Room 6: 拉桿雙分身輪流        ⭐⭐⭐
Room 7: 殘骸登場（主動犧牲）  ⭐⭐⭐
Room 8: 拉桿節奏配合          ⭐⭐⭐⭐
Room 9: 殘骸壓機關            ⭐⭐⭐⭐
Room 10: 多機制總和            ⭐⭐⭐⭐⭐
```

**節奏原則**：每兩房介紹一個新機制、之間夾一房深化練習。難度上下交錯、不單調上爬。

---

## 六、謎題評估 Checklist

設計新房間時、用此清單自我檢查：

### 機制清晰度
- [ ] 一個房間最多教**一個新機制**（複習舊機制不算新）
- [ ] 玩家可以從場景視覺猜出大致目標（不需要文字才懂）
- [ ] 新機制有明顯視覺差異（如拉桿 vs 按鈕長相不同）

### 操作可行性
- [ ] 所有跳躍距離 ≤ 100px 垂直 / ≤ 200px 水平（依 Player 物理）
- [ ] 分身重播 ≤ 30s（保證玩家不需錄超長）
- [ ] 玩家可以**重做**任何步驟而不被永久卡住

### 解法明確性
- [ ] 至少有一條「**直覺解法**」（即使笨方法也能過）
- [ ] 高手可有「**優雅解法**」（少召喚 / 少分身 / 少時間）
- [ ] 沒有「**唯一解**」逼玩家像神諭一樣猜中

### 失敗成本
- [ ] 死亡 → 重生於最近 checkpoint、不損失槽位錄製
- [ ] 槽位錯了 → 可重錄 / 刪除
- [ ] 殘骸亂噴 → 重生時 zone 內自動清理

### 主題傳達
- [ ] 「合作 / 共存」感覺有沒有變強？（vs 純 puzzle）
- [ ] 死亡有「儀式感」還是純失敗？（殘骸視覺加分）
- [ ] 玩家自己錄製的內容會不會讓他「驚覺」？（emergent gameplay）

---

## 七、未來機制候選清單

這些還沒設計、之後想加的（按優先序）：

1. **可推箱子 / 重物**：Player+Clone 合力推到位置
2. **計時平台**：踩上後 N 秒消失（強迫快速通過）
3. **方向性傳送門**：A → B 單向、可用分身在 B 端等待
4. **可破壞地板**：踩破會墜下、可故意破壞讓殘骸通過
5. **時間鎖定區域**：區域內錄製/召喚行為被限制
6. **共振光點**：兩個光點相鄰時其中一個召喚另一個自動召喚
7. **影像反向**：召喚時可選「鏡像」讓分身向反方向走
8. **音效感應器**（要做音效系統）：跳躍 / 落地有聲、機關感應

---

## 八、待後期決定（懸而未決）

- 整個遊戲的長度目標（10 房？30 房？章節結構？）
- 故事 / 敘事：分身與本體的關係是什麼設定？光點痕跡的世界觀含義？
- 美術風格定案（目前色塊原型）
- 音效 / 音樂方向
- 結局：通關後玩家看到什麼？
- 難度模式（easy / normal / hard）
- Speed-run 友善度

---

## 九、版本歷程

| 日期 | 變更 |
|---|---|
| 2026-05-02 | 文件創建。包含 Room 5-10 提案、難度評估 checklist、機制工具箱盤點 |
| 2026-05-02 | 新增 PressCounter 機制（計次門）+ Room 6A 計次門提案；PressButton 加壓下動畫 |
| 2026-05-02 | Room 6 實作：target=50、track_all_buttons、reset_on_checkpoint；Exit 移到 Room 6；Checkpoint 加 touched signal |
| 2026-05-02 | 新增「跨房共用機制延伸方向」章節（A1-A3 / B1-B3 / C1-C4）+ 設計約定「閾值 ≥ 即可、非密碼鎖」 |
