# 平行時空合作遊戲 — 實作用規格書

> **工程藍圖**。橋接 [`設計構想.md`](設計構想.md)（玩家視角的遊戲樣貌）與 [`實作參考內容.md`](實作參考內容.md)（設計分析）。
> 對應 M4-M10 開發階段。
> 版本：2026-05-02

---

## 一、文件目的與現況

### 1.1 規格書定位

| 文件 | 描述什麼 | 視角 |
|---|---|---|
| [`設計構想.md`](設計構想.md) | 遊戲玩起來該長什麼樣 | 玩家 |
| [`實作參考內容.md`](實作參考內容.md) | 主題契合度與架構分析 | 設計師 |
| **本文件** | **怎麼做、做到什麼程度算過關、需要多久** | **工程師** |

### 1.2 已完成階段（M0-M3）

| 階段 | 內容 | 狀態 |
|---|---|---|
| M0 | 走跳手感調校（含不對稱重力、coyote/buffer）| ✅ |
| M1 | 錄製系統骨架 | ✅ |
| M2 | 召喚與重播 | ✅ |
| M2.5 | 碰撞層、召喚 CD、platform_on_leave 修正 | ✅ |
| M3 | 第一個必經教學謎題 + Hint 系統 | ✅（M10 重做）|

現有檔案位於 `godot/`：11 個 .gd 腳本 + 5 個 .tscn 場景。

### 1.3 待實作系統清單（S1-S15）

| # | 系統 | 狀態 | milestone |
|---|---|---|---|
| S1 | PlayerController autoload | 全新 | M4 |
| S2 | Player 本體凍結 | 修改 | M4 |
| S3 | RecordingProxy 類別 | 全新 | M4 |
| S4 | 視覺儀式系統 | 全新 | M4 + M10 |
| S5 | LightPoint 系統 | 全新 | M5 |
| S6 | 5 槽位管理 + FIFO | 修改 | M5 |
| S7 | 光點互動（R 重錄、Q 刪除）| 全新 | M6 |
| S8 | Frozen Clone 凍結機制 | 修改 | M7 |
| S9 | PhysicalCarcass + CarcassTrace | 全新 | M8 |
| S10 | CarcassManager autoload | 全新 | M8 |
| S11 | Hazard 系統 | 全新 | M8 |
| S12 | TriggerZone 系統 | 全新 | M9 |
| S13 | 本體死亡重置事件 | 全新 | M9 |
| S14 | HUD 更新 | 重寫 | M6 |
| S15 | RecordingProxy 死亡 A3 整合 | 跨系統 | M9 |

### 1.4 文件交叉索引慣例

- 對應設計細節 → 「**設計**: [`設計構想.md`](設計構想.md) 第 X 節」
- 對應主題分析 → 「**分析**: [`實作參考內容.md`](實作參考內容.md) 第 X 段」
- 既有檔案 → 「**檔案**: `godot/...`」

---

## 二、各系統規格

每個系統含四個區塊：**開發目標**、**開發需求**、**技術建議**、**驗收清單**。

---

### S1 — PlayerController autoload

**設計**: [`設計構想.md`](設計構想.md) 第三節 1（錄製）整體流程
**分析**: [`實作參考內容.md`](實作參考內容.md) 1.3「誰在什麼時候讀輸入」

#### 開發目標
解決「**錄製狀態散落多處**」的架構問題。讓 Player 本體、RecordingProxy、Camera、儀式時序由單一節點統籌。
避免錄製邏輯分散在 Player.gd / RecordingManager.gd / 場景根，造成 bug 溫床。

#### 開發需求
- autoload Node，全域唯一實例
- 對外 API：
  - `start_recording(player: Player)` — 觸發錄製儀式
  - `end_recording()` — 結束錄製儀式
  - `is_recording() -> bool` — 查詢狀態
  - `get_active_player() -> Node2D` — 回傳當前可控節點（本體 or RecordingProxy）
  - `handle_recording_proxy_death(position: Vector2)` — 處理 A3
- 內部狀態：
  - `state: enum { NORMAL, RECORDING }`
  - 引用：當前 Player、當前 RecordingProxy、當前 Camera target
- 對外 signal：
  - `recording_started`、`recording_ended`
  - `state_changed`
- 場景切換時清理引用（避免跨場景殘留）

#### 技術建議
- `extends Node`（不必 Node2D）
- 在 [`project.godot`](godot/project.godot) 註冊為 autoload
- 不直接 add_child 場景物件，改用 `Player.get_parent()` 或透過場景根訂閱 signal
- 範例最小骨架：

```gdscript
extends Node
signal recording_started
signal recording_ended

enum State { NORMAL, RECORDING }
var state: State = State.NORMAL
var current_player: Player = null
var current_proxy: RecordingProxy = null

func start_recording(player: Player) -> void:
    if state != State.NORMAL: return
    state = State.RECORDING
    current_player = player
    # ... 凍結 player、生成 proxy、啟動儀式
    recording_started.emit()
```

#### 驗收清單
- [ ] autoload 已註冊，`PlayerController` 在任意腳本可用
- [ ] start_recording / end_recording 各呼叫一次後狀態正確切換
- [ ] 重複呼叫 start_recording 在 RECORDING 狀態下被忽略
- [ ] 場景切換時不殘留前場景的引用
- [ ] **主題**: 所有錄製相關事件由 PlayerController 統籌，其他節點透過 signal 訂閱

---

### S2 — Player 本體凍結

**設計**: [`設計構想.md`](設計構想.md) 第三節 1「為什麼本體留原地」
**分析**: [`實作參考內容.md`](實作參考內容.md) 1.5.1（本體位置處理）+ 1.5.2（空中按 R）

#### 開發目標
錄製期間本體留原地、視覺半透明、無物理（不被機關殺）。
這是「**本體留原地**」設計的程式實現。

#### 開發需求
- Player.gd 加 `is_frozen: bool` 變數
- 凍結時：
  - `_physics_process` 早退
  - CollisionShape disabled 或 collision_layer/mask = 0（不被機關偵測）
  - modulate.a = 0.5（視覺半透明，由 RitualPlayer Tween 控制）
  - 不受重力影響（在空中按 R 會懸浮）
- 解凍時：
  - 恢復 _physics_process、collision、modulate、重力
- 凍結期間玩家本體**完全無敵**（不受任何傷害）

#### 技術建議
- `is_frozen` 為簡單 bool flag，避免引入完整狀態機
- `_physics_process` 早退而非 `set_physics_process(false)`，因為要在凍結中仍能接收 RitualPlayer 的 modulate Tween
- 凍結 / 解凍由 PlayerController 呼叫
- collision 用 `set_collision_layer(0)` + `set_collision_mask(0)`（恢復時記得存原值）

```gdscript
# Player.gd 新增
var is_frozen := false
var _saved_collision_layer: int = 0
var _saved_collision_mask: int = 0

func freeze():
    is_frozen = true
    _saved_collision_layer = collision_layer
    _saved_collision_mask = collision_mask
    set_collision_layer(0)
    set_collision_mask(0)

func unfreeze():
    is_frozen = false
    set_collision_layer(_saved_collision_layer)
    set_collision_mask(_saved_collision_mask)

func _physics_process(delta):
    if is_frozen:
        return
    # ... 既有邏輯
```

#### 驗收清單
- [ ] is_frozen=true 時，玩家按方向鍵本體不動
- [ ] 凍結期間機關（hazard）不能殺死本體
- [ ] 凍結期間在空中不會掉落（重力暫停）
- [ ] 凍結期間 modulate.a = 0.5（半透明）
- [ ] 解凍後本體完全恢復原物理、原視覺、原碰撞
- [ ] **主題**: 玩家看見本體靜止在分裂點，視覺上是「等待中的時間線」

---

### S3 — RecordingProxy 類別

**設計**: [`設計構想.md`](設計構想.md) 第三節 1「新分身出現在本體位置」
**分析**: [`實作參考內容.md`](實作參考內容.md) 1.3「拆出 RecordingProxy 類別」

#### 開發目標
玩家在錄製期間操控的可移動分身。**繼承 Player.gd**（共用所有走跳邏輯），但不被加入 player_real group、輸入會被 RecordingManager 記下。

#### 開發需求
- 新類別 `RecordingProxy extends Player`
- _ready：
  - **不**加入 player_real group（避免 Exit 等場景物件誤判它是真本體）
  - 設視覺色（淡藍灰，與正常 Clone 同色但 100% alpha）
  - collision_mask 排除 layer 2（凍結分身的層）— 實作 A1 規則
- 沿用 Player 的 _read_inputs（讀鍵盤）與 _record_frame_if_needed（記錄到 RecordingManager）
- 結束錄製時 queue_free（PlayerController 呼叫）

#### 技術建議
- 新增 `RecordingProxy.tscn` 場景，與 Player.tscn 同結構但 script 不同
- 視覺色與 Clone 同（淡藍灰）——讓玩家看到的是「我在另一條時間線中走」
- 不需要新加 `class_name`，但可以加方便辨識

```gdscript
extends Player
class_name RecordingProxy

func _ready():
    spawn_position = position
    visual.color = Color(0.4, 0.7, 1.0, 1.0)  # 淡藍灰、滿透明度
    collision_mask = 1  # 只偵測 World，不偵測 Clone (layer 2)
    # 不加入 player_real group
```

#### 驗收清單
- [ ] 按 R 後生成的 RecordingProxy 可被玩家鍵盤控制
- [ ] RecordingProxy 走跳手感與 Player 完全一致
- [ ] RecordingProxy 走過 Exit area 不會觸發通關
- [ ] 結束錄製 RecordingProxy 消失
- [ ] RecordingProxy 撞凍結分身會穿透（A1 規則）
- [ ] **主題**: 玩家感覺自己控制的是「岔出去的時間線」，不是本體

---

### S4 — 視覺儀式系統

**設計**: [`設計構想.md`](設計構想.md) 第三節 1「視覺過渡」
**分析**: [`實作參考內容.md`](實作參考內容.md) 1.4「視覺儀式的時間軸」+ 1.6「最低投資」

#### 開發目標
錄製進入與結束的視覺儀式——**主題能不能被感受到的關鍵**。
M4 做最低版（功能可用）、M10 打磨到位（自評 ≥ 7/10）。

#### 開發需求

##### M4 最低版
- **進入錄製**：
  - 暫停 0.2 秒（`get_tree().paused = true`）
  - 本體 modulate Tween 1.0 → 0.5（0.3 秒）
  - RecordingProxy 淡入 0% → 100%（0.3 秒）+ 側偏移 30 px
  - 紅框邊框淡入（0.3 秒）
  - 鏡頭瞬切到 RecordingProxy（不平滑）
- **結束錄製**：
  - 分身淡出 100% → 0%（0.4 秒）
  - 本體淡入回 100%（0.3 秒）
  - 光點淡入（0.5 秒）
  - 鏡頭瞬切回本體

##### M10 打磨版
- 進入完整 0.9 秒儀式（見 [`實作參考內容.md`](實作參考內容.md) 1.4 表格）
- 結束完整 1.0 秒儀式
- 鏡頭平滑過渡（Camera2D `position_smoothing_enabled`）
- 紅框可改膠卷邊框風格
- （可選）模糊 shader for 分身

#### 技術建議
- 新增 `RitualPlayer.gd` autoload 或場景內單例，統一管理時間軸
- 使用 Godot Tween（create_tween + tween_property）
- World freeze：`get_tree().paused = true` + RitualPlayer 設 `process_mode = ALWAYS`
- 紅框：CanvasLayer + ColorRect，邊框樣式用 stylebox 或 shader
- M10 才寫 shader

```gdscript
# RitualPlayer.gd 簡化骨架
extends Node

@onready var red_frame: ColorRect = $CanvasLayer/RedFrame

func play_recording_start_ritual(body: Player, proxy: RecordingProxy):
    get_tree().paused = true
    await get_tree().create_timer(0.2).timeout
    get_tree().paused = false
    
    var tween = create_tween().set_parallel()
    tween.tween_property(body, "modulate:a", 0.5, 0.3)
    tween.tween_property(proxy, "modulate:a", 1.0, 0.3).from(0.0)
    tween.tween_property(red_frame, "modulate:a", 1.0, 0.3).from(0.0)
    await tween.finished
```

#### 驗收清單

##### M4 最低版
- [ ] 按 R 後有可見的「儀式停頓」（>=0.2 秒）
- [ ] 本體可見地淡化、RecordingProxy 可見地淡入
- [ ] 紅框邊框出現
- [ ] 結束錄製時分身可見地淡出、本體可見地淡入

##### M10 打磨版（主題層）
- [ ] 「**按 R 的瞬間我感覺到分裂了嗎？**」自評 ≥ 7/10
- [ ] 「**結束錄製感覺像時間線完成了嗎？**」自評 ≥ 7/10
- [ ] 找朋友試玩，能否理解「我控制的是分身、不是本體」

---

### S5 — LightPoint 系統

**設計**: [`設計構想.md`](設計構想.md) 第三節 3「光點：兩種狀態」

#### 開發目標
錄製結束時在錄製起點生成「光點」——對應一個槽位、可被玩家互動。
亮光點（活躍槽位）與痕跡光點（被覆蓋的槽位）兩種狀態。

#### 開發需求
- 新類別 `LightPoint extends Area2D`
- 屬性：
  - `slot_index: int`（-1 表示痕跡）
  - `state: enum { BRIGHT, TRACE }`
- 視覺：
  - BRIGHT：亮藍/亮白圓圈、脈動動畫（modulate.a 在 0.7~1.0 間循環）
  - TRACE：暗灰圓圈、不脈動、約 25% 亮度
- 偵測範圍：圓形或矩形 area，半徑/邊長約 30 px
- body_entered/exited signal（由 PlayerController 訂閱）
- 狀態轉換：BRIGHT → TRACE 有過渡動畫（0.4 秒淡化）
- TRACE 不再轉回 BRIGHT

#### 技術建議
- Area2D + Polygon2D（地面圓圈）+ CollisionShape2D
- 脈動：Tween 循環 modulate.a
- 新增 `LightPoint.tscn` + `LightPoint.gd`
- 不直接管理槽位資料（那是 RecordingManager 的事），LightPoint 只是視覺與互動的代理

```gdscript
extends Area2D
class_name LightPoint

enum State { BRIGHT, TRACE }
var state: State = State.BRIGHT
var slot_index: int = -1

@onready var visual: Polygon2D = $Visual

func _ready():
    if state == State.BRIGHT:
        _start_pulse()

func _start_pulse():
    var tween = create_tween().set_loops()
    tween.tween_property(visual, "modulate:a", 0.7, 0.8)
    tween.tween_property(visual, "modulate:a", 1.0, 0.8)

func convert_to_trace():
    state = State.TRACE
    slot_index = -1
    var tween = create_tween()
    tween.tween_property(visual, "modulate", Color(0.5, 0.5, 0.5, 0.25), 0.4)
```

#### 驗收清單
- [ ] 結束錄製後在錄製起點看到亮光點
- [ ] 光點脈動動畫運作
- [ ] 玩家走進光點偵測範圍會 emit body_entered
- [ ] 光點被 FIFO 覆蓋時，BRIGHT → TRACE 視覺過渡 0.4 秒
- [ ] TRACE 不再脈動、亮度約 25%
- [ ] **主題**: 玩家走過自己錄製過的位置，能看到「曾經分岔過的痕跡」

---

### S6 — 5 槽位管理 + FIFO

**設計**: [`設計構想.md`](設計構想.md) 第三節 3「槽位系統」+「FIFO 自動覆蓋」

#### 開發目標
RecordingManager 從目前的「無上限存檔列表」改成「**5 個槽位**」資料結構，支援 FIFO 自動覆蓋。

#### 開發需求
- 修改 `godot/scripts/RecordingManager.gd`：
  - `slots: Array[Recording] = [null, null, null, null, null]`（固定 5 格）
  - `register_recording(rec: Recording) -> int` — 回傳被填入的 slot_index
    - 優先填空槽位（0→4）
    - 全滿則 FIFO 覆蓋槽位 0
    - FIFO 時舊 LightPoint → TRACE、舊 PhysicalCarcass → 痕跡
  - `delete_slot(slot_index: int)` — 主動刪除（Q 鍵觸發）
  - `replace_slot(slot_index: int, rec: Recording)` — 重錄（站光點上 R 觸發）
  - `get_recording(slot_index: int) -> Recording`
- 對外 signal：
  - `slot_changed(slot_index, recording)` — 通知 HUD 與 LightPointManager
  - `slot_about_to_fifo(slot_index)` — 錄製進行中通知，HUD 顯示警告
  - `slot_fifoed(slot_index)` — FIFO 完成通知

#### 技術建議
- 槽位用 Array 而非 Dictionary，順序穩定
- FIFO 規則簡單：永遠覆蓋 slot 0
- 配合 LightPointManager（autoload 或在 RecordingManager 內）追蹤每個槽位的光點實例
- 改寫時保留與 Recording 資料結構的相容（Recording.gd 不改）

#### 驗收清單
- [ ] 第 1 次錄製存到 slot 0
- [ ] 第 5 次錄製存到 slot 4
- [ ] 第 6 次錄製覆蓋 slot 0（FIFO）
- [ ] 覆蓋時舊光點淡化變痕跡（不消失）
- [ ] 玩家能查詢任一 slot 的 Recording
- [ ] HUD 收到 slot_changed signal 後正確更新顯示
- [ ] **主題**: 玩家感覺到「最舊的選擇被取代了，但不會完全消失」

---

### S7 — 光點互動（R 重錄、Q 刪除）

**設計**: [`設計構想.md`](設計構想.md) 第三節 3「光點互動」

#### 開發目標
玩家站在亮光點上：按 R 觸發重錄該槽位、按 Q 刪除該槽位。痕跡光點不可互動。

#### 開發需求
- PlayerController 偵測「玩家是否站在亮光點上」（透過 LightPoint.body_entered signal）
- 顯示上下文提示（HUD 中央偏下）：「站在槽位 [N] 的光點上 — R 重錄 / Q 刪除」
- **站亮光點上按 R**：
  - PlayerController.start_recording 進入錄製模式
  - 標記目標槽位 = 該光點 slot_index
  - RecordingProxy 出生位置 snap 到光點中心
  - 結束錄製時 RecordingManager.replace_slot 覆蓋
  - 光點留在原位
- **站亮光點上按 Q**：
  - RecordingManager.delete_slot
  - 該光點立即淡化消失（**不變痕跡**——主動刪除是玩家明確意圖）
  - 該槽位的 PhysicalCarcass 轉痕跡（與 FIFO 相同處理）
- **站痕跡光點上**：完全沒提示、R/Q 無效
- **站空地按 R**：標準新建錄製，套用 FIFO

#### 技術建議
- 在 [`project.godot`](godot/project.godot) 加新輸入動作 `delete_slot`（Q 鍵）
- HUD 加「context hint」Label，由 PlayerController 控制 fade_in/fade_out
- 多光點重疊偵測：取距離玩家最近的（PlayerController 計算 distance_squared）

#### 驗收清單
- [ ] 走進亮光點偵測範圍，HUD 出現上下文提示
- [ ] 站光點上按 R，進入重錄模式
- [ ] 重錄後光點留在原位，槽位內容更新
- [ ] 站光點上按 Q，光點消失，槽位變空
- [ ] 站痕跡光點上 R 等同空地新建錄製，Q 無效
- [ ] **主題**: 玩家對自己的錄製有「明確的修正路徑」

---

### S8 — Frozen Clone 凍結機制

**設計**: [`設計構想.md`](設計構想.md) 第三節 1「錄製期間的世界狀態」
**分析**: [`實作參考內容.md`](實作參考內容.md) 第 3 段（錄製與召喚的關係）

#### 開發目標
錄製期間既有的播放中分身**凍結成模糊剪影**，保留世界影響但不被 RecordingProxy 碰撞（A1 模型）。

#### 開發需求
- 修改 [`godot/scripts/Clone.gd`](godot/scripts/Clone.gd)：加 `freeze()` 與 `unfreeze()` 方法
- freeze 時：
  - `set_physics_process(false)`
  - modulate Tween：原色 → Color(0.5, 0.5, 0.5, 0.3)（0.3 秒）
  - collision_layer 維持原值（**仍被機關偵測**——A1 規則）
  - 內部狀態（current_frame、velocity）保留
- unfreeze 時：
  - `set_physics_process(true)`
  - modulate Tween：Color(0.5, 0.5, 0.5, 0.3) → 原色（0.3 秒）
- PlayerController.start_recording 呼叫所有 active_clone 的 freeze
- PlayerController.end_recording 呼叫 unfreeze
- RecordingProxy.collision_mask 不含 layer 2，自然穿透凍結分身

#### 技術建議
- 在 Clone.gd 新增 `_frozen_state: Dictionary` 暫存原狀態（包含 modulate）
- 用 `add_to_group("active_clone")` 在 _ready 時加，方便 PlayerController 一次 freeze 全部
- 模糊 shader 為 M10 打磨項目，M7 先用 modulate 色調與透明度

```gdscript
# Clone.gd 新增
var _frozen_state: Dictionary = {}

func freeze():
    if _frozen_state.size() > 0: return
    _frozen_state = { "modulate": modulate }
    set_physics_process(false)
    var tween = create_tween()
    tween.tween_property(self, "modulate", Color(0.5, 0.5, 0.5, 0.3), 0.3)

func unfreeze():
    if _frozen_state.size() == 0: return
    set_physics_process(true)
    var tween = create_tween()
    tween.tween_property(self, "modulate", _frozen_state.modulate, 0.3)
    _frozen_state.clear()
```

#### 驗收清單
- [ ] 錄製開始時，所有 active Clone 0.3 秒淡化為灰色剪影
- [ ] 凍結期間 Clone 不動、不執行錄製輸入
- [ ] 凍結期間 Clone 仍被按鈕偵測（按鈕保持按下、門保持開）
- [ ] 凍結期間 Clone 仍擋投射物
- [ ] 凍結期間 RecordingProxy 對 Clone 穿透
- [ ] 結束錄製後 Clone 解凍恢復、繼續播放剩餘錄製
- [ ] **主題**: 玩家看到凍結分身的時候感覺「時間真的暫停了」

---

### S9 — PhysicalCarcass + CarcassTrace

**設計**: [`設計構想.md`](設計構想.md) 第三節 4「殘骸」
**分析**: [`實作參考內容.md`](實作參考內容.md) 第 2 段（殘骸）

#### 開發目標
分身被機關殺死時形成物理殘骸；殘骸可轉痕跡（兩種樣式：新與舊）。
**主動犧牲**的視覺呈現。

#### 開發需求

##### PhysicalCarcass
- StaticBody2D + Polygon2D + CollisionShape2D
- 屬性：`slot_index`、`death_position`、`formation_time`
- 視覺：定格剪影（淡色、邊緣有微弱光暈、地面光暈）
- collision_layer：可被玩家踩、可被按鈕偵測
- 方法：`convert_to_trace()` → 生成 CarcassTrace、queue_free 自己

##### CarcassTrace
- Node2D + Polygon2D（無 collision）
- 兩種狀態：NEW（50% 亮度）、OLD（20% 亮度，輪廓更模糊）
- 自動轉換：formation_time + 60 秒後 → OLD（5 秒淡化過渡）
- 永久存在（不消失）

#### 技術建議
- PhysicalCarcass.tscn 與 CarcassTrace.tscn 分開場景檔
- CarcassTrace 用 _process 監測時間（or Timer node）觸發轉舊
- 視覺差異：物理殘骸（鮮亮）vs 痕跡（灰白）色板要明顯分開
- 殘骸視覺保留死亡瞬間的姿態（簡化版用相同剪影）

```gdscript
class_name PhysicalCarcass extends StaticBody2D

var slot_index: int
var death_position: Vector2
var formation_time: float

func _ready():
    add_to_group("physical_carcass")
    formation_time = Time.get_ticks_msec() / 1000.0

func convert_to_trace() -> CarcassTrace:
    var trace = preload("res://scenes/CarcassTrace.tscn").instantiate()
    trace.position = position
    trace.formation_time = formation_time
    get_parent().add_child(trace)
    queue_free()
    return trace
```

#### 驗收清單
- [ ] 分身撞 hazard 死，原位置出現物理殘骸
- [ ] 物理殘骸可被玩家踩、可壓按鈕
- [ ] 殘骸轉痕跡：modulate 從亮到 50%（0.5 秒）
- [ ] 60 秒後新痕跡 → 舊痕跡（5 秒淡化到 20%）
- [ ] 舊痕跡永久存在（不繼續變化）
- [ ] **主題**: 玩家看殘骸感覺「這條時間線走到死路」

---

### S10 — CarcassManager autoload

**設計**: [`設計構想.md`](設計構想.md) 第三節 4「槽位制物理殘骸」
**分析**: [`實作參考內容.md`](實作參考內容.md) 2.2 槽位制實作架構

#### 開發目標
管理「**每槽位 1 個物理殘骸**」規則。處理同槽位覆蓋、跨 zone 重置等所有殘骸生命週期事件。

#### 開發需求
- autoload Node
- 對外 API：
  - `register_death(slot_index, position, parent_node)` — Clone 死亡時呼叫
  - `clear_slot_carcass(slot_index)` — 槽位 FIFO/重錄/刪除時呼叫
  - `reset_zone(zone)` — 本體死亡時呼叫
- 內部維護：
  - `slot_carcasses: Dictionary[int, PhysicalCarcass]`（最多 5 個 entry）
- 觸發規則：
  - register_death 時若該 slot 已有殘骸 → 舊轉痕跡、新生成
  - clear_slot_carcass → 舊轉痕跡
  - reset_zone → zone 內所有 slot 殘骸 → 痕跡

#### 技術建議
- 不需要記憶體上限（痕跡無上限是設計決策）
- 與 RecordingManager 協作：FIFO/刪除/重錄事件由 RecordingManager 觸發、CarcassManager 收 signal
- 場景切換清理（避免跨場景殘留）

#### 驗收清單
- [ ] 同槽位 Clone 連續死亡，地圖只看到 1 個物理殘骸
- [ ] 不同槽位 Clone 在同位置死，2 個物理殘骸並存
- [ ] 槽位被 FIFO/Q 刪除時，該槽位殘骸自動轉痕跡
- [ ] 本體在 zone Z 死，zone Z 內所有殘骸轉痕跡，跨 zone 殘骸不變
- [ ] 物理殘骸全域上限 5（每槽位 1）

---

### S11 — Hazard 系統

**設計**: [`設計構想.md`](設計構想.md) 第三節 4「形成條件」

#### 開發目標
殺死分身的機關（尖刺、投射物等）。觸發時通知 Clone 死亡、形成殘骸。

#### 開發需求
- 新類別 `Hazard extends Area2D`
- body_entered signal：
  - Clone 進入：呼叫 Clone.die() → CarcassManager.register_death
  - RecordingProxy 進入：觸發 A3（PlayerController.handle_recording_proxy_death）
  - Player 進入：本體死亡 → 重置事件
- 多種 Hazard 類型：尖刺（靜態）、投射物（移動）、雷射（規律閃爍）
- **原型階段先做尖刺一種**

#### 技術建議
- Hazard.collision_mask = 3（偵測 Player layer 1 + Clone layer 2）
- 子類化以後加新 hazard：HazardSpike, HazardProjectile, HazardLaser

```gdscript
class_name Hazard extends Area2D

func _ready():
    body_entered.connect(_on_body_entered)

func _on_body_entered(body: Node2D):
    if body is RecordingProxy:
        PlayerController.handle_recording_proxy_death(body.position)
    elif body is Clone:
        var clone = body as Clone
        clone.die()
        CarcassManager.register_death(clone.slot_index, clone.position, get_parent())
    elif body is Player:
        body.died.emit()  # 觸發重置
```

#### 驗收清單
- [ ] 召喚 Clone 走過尖刺，Clone 死亡並留物理殘骸
- [ ] RecordingProxy 走過尖刺，A3 觸發
- [ ] Player 走過尖刺，本體死亡、觸發重置
- [ ] 殘骸（物理）走過尖刺，殘骸不會死

---

### S12 — TriggerZone 系統

**設計**: [`設計構想.md`](設計構想.md) 第三節 4「物理殘骸轉痕跡的觸發」

#### 開發目標
定義「謎題區域」邊界。本體死亡時用 zone 範圍決定哪些殘骸轉痕跡。

#### 開發需求
- 新類別 `TriggerZone extends Area2D`
- 矩形範圍（CollisionShape2D + RectangleShape2D）
- 玩家本體進入/離開 zone 時 emit signal
- 對外 API：
  - `contains(world_pos: Vector2) -> bool`
- 場景配置：每個謎題場景手動放 1 個 TriggerZone 包圍核心區域
- 玩家可同時在多個 zone 內

#### 技術建議
- TriggerZone 是純設計工具，不影響玩家操作
- 玩家可同時在多個 zone：用 Array 追蹤
- 本體死亡時取「最後進入的 zone」當作重置範圍

```gdscript
class_name TriggerZone extends Area2D

func _ready():
    body_entered.connect(_on_body_entered)
    body_exited.connect(_on_body_exited)

func contains(world_pos: Vector2) -> bool:
    # 用 CollisionShape2D 的 RectangleShape2D 計算
    var shape = $CollisionShape2D.shape as RectangleShape2D
    var local = to_local(world_pos)
    return abs(local.x) <= shape.size.x / 2 and abs(local.y) <= shape.size.y / 2
```

#### 驗收清單
- [ ] 場景中可放置 TriggerZone（編輯器中可見邊界）
- [ ] 玩家進入 zone emit body_entered
- [ ] 玩家本體死亡時，正確識別當前所在 zone
- [ ] zone.contains(pos) 對 zone 內位置回傳 true
- [ ] 跨 zone 邊界時不出現雙重觸發

---

### S13 — 本體死亡重置事件

**設計**: [`設計構想.md`](設計構想.md) 第三節 4「物理殘骸轉痕跡的觸發」B4 決策

#### 開發目標
玩家本體死亡時觸發本關重置：清除分身、轉換 zone 內物理殘骸為痕跡、本體在檢查點重生。

#### 開發需求
- PlayerController 訂閱 Player.died signal
- 重置流程：
  1. 所有 active Clone 消失（queue_free）
  2. 當前 zone 內物理殘骸 → 痕跡（CarcassManager.reset_zone）
  3. 跨 zone 殘骸不變
  4. 槽位錄製保留
  5. 本體在 zone 入口或最近檢查點重生
- 視覺：黑屏 0.3 秒、淡入重生
- **不重置**：殘骸痕跡、痕跡光點

#### 技術建議
- 重生位置：M9 階段先用「zone 入口固定點」（zone 上指定 spawn_point Marker2D）
- 黑屏：CanvasLayer + ColorRect + Tween

#### 驗收清單
- [ ] Player 撞 hazard 死亡，畫面短暫黑屏
- [ ] 重生後 active Clone 全部消失
- [ ] 當前 zone 內物理殘骸全部變痕跡
- [ ] 跨 zone 殘骸（亮的、痕跡的）不變
- [ ] 槽位錄製保留可重新召喚
- [ ] **主題**: 玩家感覺「這次失敗了，但世界記得我所有的犧牲」

---

### S14 — HUD 更新（5 槽位 + 上下文提示）

**設計**: [`設計構想.md`](設計構想.md) 第三節 3「自動命名與 HUD」+「光點互動」

#### 開發目標
重寫現有 [`HUD.gd`](godot/scripts/HUD.gd) 顯示新的 5 槽位資訊，加上下文提示（站光點上時顯示）。

#### 開發需求
- HUD 主顯示（畫面角落）：

```
┌─ 錄製槽位 ──────────────────────┐
│ [1] @(200, 645)   3.2s           │
│ [2] @(560, 420)   1.8s           │
│ [3] —                             │
│ [4] —                             │
│ [5] —                             │
└───────────────────────────────────┘
```

- 每行：槽位編號 + 自動命名（座標 + 時長）+ 空槽顯示「—」
- FIFO 警告：要被覆蓋的槽位列加「⚠ 即將被覆蓋」
- 上下文提示（中央偏下）：站光點上時顯示「站在槽位 [N] 的光點上 — R 重錄 / Q 刪除」
- 移除既有 HintManager（M10 第一謎題重做時整合）

#### 技術建議
- HUD.gd 改寫，從訂閱 RecordingManager signals 取得槽位資料
- 上下文提示用 fade_in/fade_out Tween（避免閃跳）
- 字體不必精緻，原型用 Godot 預設

#### 驗收清單
- [ ] 槽位列表正確顯示 1-5 槽
- [ ] 空槽顯示「—」
- [ ] 滿槽顯示自動命名（座標 + 時長）
- [ ] FIFO 進行中時，目標槽位有警告標記
- [ ] 站光點上時上下文提示淡入
- [ ] 離開光點時上下文提示淡出

---

### S15 — RecordingProxy 死亡（A3）整合

**設計**: [`設計構想.md`](設計構想.md) 第三節 4「形成條件」中的 RecordingProxy 死亡
**分析**: [`實作參考內容.md`](實作參考內容.md) 1.5.4 + A3 決策

#### 開發目標
RecordingProxy 撞 hazard 死亡時：錄製存到槽位（含死亡 frame）、形成物理殘骸、結束儀式啟動。

#### 開發需求
- Hazard 偵測 RecordingProxy → 呼叫 PlayerController.handle_recording_proxy_death
- 流程：
  1. RecordingManager 結束錄製（含當前 frame 為死亡 frame）→ 存槽位
  2. CarcassManager.register_death 在死亡位置生成物理殘骸
  3. 結束錄製儀式（與正常結束相同的視覺淡入淡出）
  4. 本體解凍恢復控制
- 最短錄製時長 0.5 秒（30 frames @ 60Hz）：低於不存槽位

#### 技術建議
- 跨 Hazard / PlayerController / RecordingManager / CarcassManager
- 使用 signal 而非直接呼叫，降低耦合

#### 驗收清單
- [ ] 錄製中 RecordingProxy 撞尖刺，錄製結束儀式啟動
- [ ] 該錄製被存到槽位（含死亡 frame）
- [ ] 死亡位置出現物理殘骸（屬於該槽位）
- [ ] 本體解凍恢復
- [ ] 之後召喚此錄製，Clone 重複此路徑死於同位置（覆蓋舊殘骸成痕跡）
- [ ] 錄製時長 < 0.5 秒時不存槽位、不形成殘骸

---

## 三、開發里程碑（M4-M10）

每個 milestone 含：目標、關鍵任務、新增/修改檔案、驗收檢查、估時、依賴。

---

### M4 — 錄製儀式骨架

| 項目 | 內容 |
|---|---|
| 涵蓋系統 | S1, S2, S3, S4（最低版） |
| 估時 | **1-2 週** |
| 前置依賴 | M3 完成 |
| 後續依賴 | M5 需要本 milestone 的 PlayerController |

#### 目標
把現有「玩家邊走邊錄」改成「按 R → 本體留原地、新分身出現、玩家操控分身」。
最低版視覺儀式可運作。

#### 關鍵任務
1. 創建 PlayerController autoload
2. 修改 Player.gd：加 is_frozen flag、_physics_process 早退邏輯
3. 創建 RecordingProxy.gd + RecordingProxy.tscn
4. 連結 PlayerController 與 RecordingManager
5. M4 最低版 RitualPlayer：暫停 0.2s、淡入淡出 0.3s、紅框
6. 本體凍結時 collision/物理處理

#### 新增檔案
- `godot/scripts/PlayerController.gd`
- `godot/scripts/RecordingProxy.gd`
- `godot/scenes/RecordingProxy.tscn`
- `godot/scripts/RitualPlayer.gd`

#### 修改檔案
- `godot/scripts/Player.gd` — 加 freeze/unfreeze、凍結時邏輯
- `godot/project.godot` — 加 PlayerController autoload

#### 驗收檢查
- [ ] 按 R 後本體靜止半透明、RecordingProxy 出現可控
- [ ] 玩家走 RecordingProxy 期間 RecordingManager 正常記錄
- [ ] 按結束鍵後 RecordingProxy 消失、本體恢復
- [ ] 全程不崩潰、輸入正確路由
- [ ] 自評：「我感受到本體與分身的分離了嗎？」≥ 6/10（最低版可接受）

---

### M5 — 光點系統 + 5 槽位

| 項目 | 內容 |
|---|---|
| 涵蓋系統 | S5, S6 |
| 估時 | **1 週** |
| 前置依賴 | M4 |
| 後續依賴 | M6 需要 LightPoint |

#### 目標
錄製結束時生成光點。RecordingManager 改為 5 槽位 + FIFO。

#### 關鍵任務
1. 創建 LightPoint 場景與類別
2. 改寫 RecordingManager 為 5 槽位
3. FIFO 邏輯（含舊光點轉痕跡）
4. PlayerController 在 end_recording 時呼叫光點生成
5. 通過 signal 通知 HUD 槽位變化

#### 新增檔案
- `godot/scripts/LightPoint.gd`
- `godot/scenes/LightPoint.tscn`

#### 修改檔案
- `godot/scripts/RecordingManager.gd` — 5 槽位 + FIFO
- `godot/scripts/PlayerController.gd` — 加光點生成邏輯

#### 驗收檢查
- [ ] 錄製結束後光點出現在錄製起點
- [ ] 5 個錄製後第 6 個觸發 FIFO，槽位 0 光點轉痕跡
- [ ] 痕跡光點不脈動、亮度 25%
- [ ] HUD（既有版）顯示槽位 1-5 標籤

---

### M6 — 光點互動 + HUD 重寫

| 項目 | 內容 |
|---|---|
| 涵蓋系統 | S7, S14 |
| 估時 | **1 週** |
| 前置依賴 | M5 |
| 後續依賴 | M10 視覺打磨會再動 HUD |

#### 目標
站光點上的 R 重錄、Q 刪除互動。HUD 重寫為 5 槽位顯示 + 上下文提示。

#### 關鍵任務
1. PlayerController 偵測站光點事件
2. R 重錄邏輯：snap 到光點中心、覆蓋對應槽位
3. Q 刪除邏輯
4. HUD 重寫：5 槽位列表 + 上下文提示
5. 改寫 [`CloneSpawner.gd`](godot/scripts/CloneSpawner.gd) 改用槽位索引召喚

#### 修改檔案
- `godot/scripts/CloneSpawner.gd` — 改用槽位索引
- `godot/scripts/HUD.gd` + `godot/scenes/HUD.tscn` — 重寫
- `godot/scripts/PlayerController.gd` — 加光點互動處理
- `godot/project.godot` — 加 delete_slot 輸入動作

#### 驗收檢查
- [ ] 站亮光點上按 R 進入重錄、覆蓋該槽位
- [ ] 站亮光點上按 Q 刪除、光點消失
- [ ] 站痕跡光點上無互動
- [ ] HUD 5 槽位顯示正確、上下文提示淡入淡出

---

### M7 — Frozen Clone

| 項目 | 內容 |
|---|---|
| 涵蓋系統 | S8 |
| 估時 | **0.5-1 週** |
| 前置依賴 | M4, M6 |

#### 目標
錄製期間既有分身凍結成模糊剪影、保留世界影響。

#### 關鍵任務
1. Clone.gd 加 freeze/unfreeze
2. PlayerController 在 start/end_recording 呼叫所有 active Clone
3. RecordingProxy 的 collision_mask 排除 layer 2

#### 修改檔案
- `godot/scripts/Clone.gd`
- `godot/scripts/RecordingProxy.gd`
- `godot/scripts/PlayerController.gd`

#### 驗收檢查
- [ ] 錄製開始所有 active Clone 0.3 秒淡化為灰色剪影
- [ ] 凍結中 Clone 仍壓按鈕（門保持開啟）
- [ ] RecordingProxy 對凍結 Clone 穿透
- [ ] 結束錄製 Clone 解凍恢復播放

---

### M8 — 殘骸系統

| 項目 | 內容 |
|---|---|
| 涵蓋系統 | S9, S10, S11 |
| 估時 | **1.5-2 週** |
| 前置依賴 | M7 |

#### 目標
分身被機關殺死形成殘骸；CarcassManager 槽位制；新/舊痕跡兩種樣式。

#### 關鍵任務
1. PhysicalCarcass + CarcassTrace 場景與類別
2. CarcassManager autoload
3. Hazard 類別 + 場景（先做尖刺）
4. Clone.gd 加 die() 方法、Hazard 偵測 Clone 觸發
5. CarcassTrace 的新/舊兩階段視覺
6. PressButton 偵測 PhysicalCarcass

#### 新增檔案
- `godot/scripts/PhysicalCarcass.gd` + `godot/scenes/PhysicalCarcass.tscn`
- `godot/scripts/CarcassTrace.gd` + `godot/scenes/CarcassTrace.tscn`
- `godot/scripts/CarcassManager.gd`
- `godot/scripts/Hazard.gd` + `godot/scenes/HazardSpike.tscn`

#### 修改檔案
- `godot/scripts/Clone.gd` — 加 die()
- `godot/scripts/PressButton.gd` — filter 加上 StaticBody2D（殘骸可壓）
- `godot/project.godot` — 加 CarcassManager autoload

#### 驗收檢查
- [ ] Clone 撞尖刺死，原位置出現物理殘骸
- [ ] 殘骸可踩、可壓按鈕
- [ ] 同槽位 Clone 連續死，地圖只看到 1 個物理殘骸
- [ ] 60 秒後痕跡從新（50%）變舊（20%）
- [ ] 主動犧牲玩法可運作

---

### M9 — TriggerZone + 重置 + A3

| 項目 | 內容 |
|---|---|
| 涵蓋系統 | S12, S13, S15 |
| 估時 | **1 週** |
| 前置依賴 | M8 |

#### 目標
TriggerZone 系統。本體死亡觸發 zone 範圍重置。RecordingProxy 死亡（A3）整合。

#### 關鍵任務
1. TriggerZone 類別與場景
2. PlayerController 訂閱 Player.died signal
3. CarcassManager.reset_zone 邏輯
4. A3：Hazard 偵測 RecordingProxy → 結束錄製儀式
5. 最短錄製時長 0.5 秒判斷

#### 新增檔案
- `godot/scripts/TriggerZone.gd` + `godot/scenes/TriggerZone.tscn`

#### 修改檔案
- `godot/scripts/Player.gd` — 加 died signal
- `godot/scripts/PlayerController.gd` — 加重置事件處理 + A3
- `godot/scripts/Hazard.gd` — 偵測 Player / RecordingProxy 分流

#### 驗收檢查
- [ ] Player 死亡觸發黑屏 + 重生
- [ ] 重生時當前 zone 內殘骸轉痕跡，跨 zone 不變
- [ ] active Clone 全部消失
- [ ] RecordingProxy 撞刺，錄製存槽位 + 殘骸生成
- [ ] 0.5 秒以內的錄製不存

---

### M10 — 視覺儀式打磨 + 第一謎題重做

| 項目 | 內容 |
|---|---|
| 涵蓋系統 | S4 提升 + 跨系統整合 |
| 估時 | **1.5-2 週** |
| 前置依賴 | M9 |

#### 目標
視覺儀式從 M4 最低版升級到完整版。第一謎題重做以對應新機制。

#### 關鍵任務
1. RitualPlayer 升級：完整 0.9/1.0 秒儀式
2. 鏡頭平滑過渡（Camera2D position_smoothing）
3. 紅框升級為膠卷邊框風格
4. （可選）模糊 shader for Frozen Clone
5. FirstPuzzle.tscn + .gd 重做：適應新機制（本體留原地、5 槽位等）
6. 移除 HintManager（被新 HUD 上下文提示取代）

#### 修改檔案
- `godot/scripts/RitualPlayer.gd` — 升級儀式
- `godot/scenes/FirstPuzzle.tscn` — 重做
- `godot/scripts/FirstPuzzle.gd` — 重做
- 移除：`godot/scripts/HintManager.gd`

#### 驗收檢查
- [ ] 完整儀式時長感受對（自評 ≥ 7/10）
- [ ] 第一謎題用新機制可解
- [ ] 找朋友試玩，30 分內理解核心機制
- [ ] **主題**: 「我感受到時間分裂了」自評 ≥ 7/10

---

## 四、整體驗收清單（端對端場景）

跨 milestone 完成後的整合驗收。對應 [`實作參考內容.md`](實作參考內容.md) 各段場景演練的整合版。

### A. 完整錄製→召喚循環
- [ ] 走到位置 P，按 R → 本體留原地、RecordingProxy 出現
- [ ] 用 RecordingProxy 走幾步、跳一下
- [ ] 按結束鍵 → RecordingProxy 消失、本體恢復、P 處出現亮光點
- [ ] 走離 P
- [ ] 按 1 → 從 P 召喚 Clone、Clone 重現剛才動作、播完消散

### B. 5 槽位 FIFO
- [ ] 連錄 6 次、第 6 次觸發 FIFO
- [ ] 槽位 1 的舊光點變痕跡（不消失）
- [ ] HUD 顯示「槽位 1 已被覆蓋」

### C. 凍結中分身（A1）
- [ ] 召喚 Clone 壓按鈕、門開
- [ ] 按 R → Clone 變灰色模糊剪影、凍結
- [ ] 此時門仍開（A1 規則）
- [ ] RecordingProxy 走過 Clone 穿透
- [ ] 結束錄製 → Clone 解凍恢復播放

### D. 殘骸槽位制
- [ ] 召喚 Clone 撞尖刺死 → 物理殘骸於 P
- [ ] 再召喚同槽位 Clone 撞同尖刺死 → 物理殘骸於 P，舊轉痕跡
- [ ] 不同槽位 Clone 撞同尖刺死 → 兩個物理殘骸並存於 P

### E. 痕跡新/舊轉換
- [ ] 殘骸轉痕跡瞬間：50% 亮度（新痕跡）
- [ ] 60 秒後：開始 5 秒淡化
- [ ] 65 秒後：20% 亮度（舊痕跡）
- [ ] 之後永久保持 20% 亮度

### F. 本體死亡重置
- [ ] 在 zone Z 內，Player 撞尖刺死 → 黑屏重生
- [ ] zone Z 內所有物理殘骸轉痕跡
- [ ] 跨 zone 殘骸不變
- [ ] active Clone 全部消失
- [ ] 5 個槽位的錄製仍在

### G. RecordingProxy 死亡（A3）
- [ ] 錄製中 RecordingProxy 撞尖刺死 → 結束儀式啟動
- [ ] 該錄製存到槽位（含死亡 frame）
- [ ] 死亡位置出現物理殘骸
- [ ] 之後召喚此錄製：Clone 重複動作於同位置死

### H. 主題層驗收（自評）
- [ ] 「按 R 的瞬間我感覺到分裂了嗎？」≥ 7/10
- [ ] 「結束錄製感覺像時間線完成了嗎？」≥ 7/10
- [ ] 「分身犧牲時我感覺到失去了嗎？」≥ 7/10
- [ ] 「世界記得我嗎？」（看到痕跡時）≥ 7/10

### I. 第一謎題試玩驗收
- [ ] 找 1-2 個朋友試玩，30 分內理解核心機制
- [ ] 觀察「**啊哈**」時刻是否發生
- [ ] 玩家自然發現「主動犧牲」的可能性

---

## 五、風險與依賴

### 5.1 主要風險清單

| 風險 | 影響 | 緩解策略 | 對應 milestone |
|---|---|---|---|
| 視覺儀式重量不足、主題斷裂 | 整個遊戲被讀為「分身術」而非「時間分岔」 | M4 做最低版即時驗證、M10 打磨；自評 ≥ 7/10 才往下走 | M4, M10 |
| 凍結剪影視覺與舊痕跡混淆 | 玩家分不出「凍結分身」與「舊殘骸痕跡」 | 嚴格色板區分（凍結=灰、舊痕跡=接近黑白）、試玩確認 | M7 |
| 槽位事件 hook 散落（bug 溫床）| 「FIFO 時忘記轉殘骸成痕跡」這類 bug | 集中到 RecordingManager 統一發 signal、CarcassManager 統一接 | M5, M8 |
| 跨 zone 物理殘骸累積影響後期 | 玩家解過 5 個謎題後地圖散落物理殘骸干擾後期 | 鼓勵玩家解開後重錄/刪除槽位 | M8 |
| 主題傳達 30-40% 視覺品質依賴 | 工程做完了但主題沒被感受到 | M10 視覺打磨優先級高，必要時延期其他 milestone | M10 |
| 開發週期低估（業餘時間） | 7 個 milestone 可能拉到 3-4 個月 | 每 milestone 結束誠實檢視時間、若超過 2 週停下來檢討 scope | 全程 |
| 凍結期間「進行中事件」邏輯複雜 | 投射物、門開到一半等狀態的處理 | 投射物視為世界物件不凍結、門邏輯保持自然演進 | M7 |
| RecordingProxy 世界影響的「消失時機」 | 它按的按鈕在它消失時釋放 | 沿用「召喚 Clone 消散」的同邏輯（既有 M2 處理） | M9 |
| 大量痕跡的記憶體成本 | 玩到後期可能 100+ 痕跡 | 原型階段不擔心，若實際撐不住再加全域上限 | 後期 |

### 5.2 系統依賴圖

```
M4 (PlayerController + RecordingProxy + 儀式骨架)
  └─→ M5 (LightPoint + 5 槽位)
        └─→ M6 (光點互動 + HUD)
              └─→ M7 (Frozen Clone)
                    └─→ M8 (殘骸系統)
                          └─→ M9 (TriggerZone + 重置 + A3)
                                └─→ M10 (視覺打磨 + 第一謎題重做)
```

每個 milestone 嚴格依賴前一個。**順序不可調換**。

### 5.3 回退策略

| 情境 | 對策 |
|---|---|
| M4 視覺儀式做不出主題感 | 暫停加機制、先打磨 M4 直到自評 ≥ 7/10 |
| M8 殘骸系統過於複雜 | 暫先不做主動犧牲謎題，到 M10 才整合 |
| M10 第一謎題重做超時 | 保留 M3 既有版本作為臨時可玩版本 |
| 任何 milestone 超過 2 週 | 停下來檢討 scope、考慮拆分 |

---

## 六、不在本文件範圍

以下事項由其他文件或後期決定：

- 美術資源具體規格（顏色 RGB、字體、圖示樣式）
- 音效音樂設計（在「待後期決定」清單）
- 結局與環境敘事
- 中後期關卡設計（前 5 個教學謎題在本文件，第 6+ 由日後另寫關卡設計文件）
- 多人模式 / 線上 / 平台移植
- 模糊 shader 的具體實作（M10 之後 polish）

---

## 七、總時長估計

| 階段 | 估時 | 累計 |
|---|---|---|
| M4 | 1-2 週 | 1-2 週 |
| M5 | 1 週 | 2-3 週 |
| M6 | 1 週 | 3-4 週 |
| M7 | 0.5-1 週 | 3.5-5 週 |
| M8 | 1.5-2 週 | 5-7 週 |
| M9 | 1 週 | 6-8 週 |
| M10 | 1.5-2 週 | 7.5-10 週 |

**核心估時：7.5-10 週**（業餘每週 10-20 小時）。
打磨與意外因素估 +30-50% buffer：**10-15 週實際**。

對應日曆估計：**2.5-4 個月**。

---

## 附錄：與既有檔案的對應

### 既有檔案的修改清單

| 檔案 | M4 | M5 | M6 | M7 | M8 | M9 | M10 |
|---|---|---|---|---|---|---|---|
| [`godot/scripts/Player.gd`](godot/scripts/Player.gd) | 加 freeze | | | | | 加 died signal | |
| [`godot/scripts/RecordingManager.gd`](godot/scripts/RecordingManager.gd) | | 5 槽位 + FIFO | | | | | |
| [`godot/scripts/CloneSpawner.gd`](godot/scripts/CloneSpawner.gd) | | | 改用槽位索引 | | | | |
| [`godot/scripts/Clone.gd`](godot/scripts/Clone.gd) | | | | 加 freeze | 加 die | | |
| [`godot/scripts/HUD.gd`](godot/scripts/HUD.gd) | | | 重寫 | | | | |
| [`godot/scenes/HUD.tscn`](godot/scenes/HUD.tscn) | | | 重寫 | | | | |
| [`godot/scripts/PressButton.gd`](godot/scripts/PressButton.gd) | | | | | filter 加殘骸 | | |
| [`godot/scripts/FirstPuzzle.gd`](godot/scripts/FirstPuzzle.gd) | | | | | | | 重做 |
| [`godot/scenes/FirstPuzzle.tscn`](godot/scenes/FirstPuzzle.tscn) | | | | | | | 重做 |
| [`godot/scripts/HintManager.gd`](godot/scripts/HintManager.gd) | | | | | | | 移除 |
| [`godot/project.godot`](godot/project.godot) | autoload | | input | | autoload | | |

### 完整新增檔案清單

| 檔案 | milestone |
|---|---|
| `godot/scripts/PlayerController.gd` | M4 |
| `godot/scripts/RecordingProxy.gd` | M4 |
| `godot/scenes/RecordingProxy.tscn` | M4 |
| `godot/scripts/RitualPlayer.gd` | M4 |
| `godot/scripts/LightPoint.gd` | M5 |
| `godot/scenes/LightPoint.tscn` | M5 |
| `godot/scripts/PhysicalCarcass.gd` | M8 |
| `godot/scenes/PhysicalCarcass.tscn` | M8 |
| `godot/scripts/CarcassTrace.gd` | M8 |
| `godot/scenes/CarcassTrace.tscn` | M8 |
| `godot/scripts/CarcassManager.gd` | M8 |
| `godot/scripts/Hazard.gd` | M8 |
| `godot/scenes/HazardSpike.tscn` | M8 |
| `godot/scripts/TriggerZone.gd` | M9 |
| `godot/scenes/TriggerZone.tscn` | M9 |

**總計**：8 個新腳本 + 7 個新場景 = 15 個新檔案。

---

*文件版本：基於 設計構想.md 與 實作參考內容.md 整合 2026-05-02*
